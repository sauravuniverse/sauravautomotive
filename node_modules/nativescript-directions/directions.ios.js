"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("tns-core-modules/utils/utils");
const directions_common_1 = require("./directions.common");
const isAppAvailable = require("nativescript-appavailability").availableSync;
class Directions extends directions_common_1.DirectionsCommon {
    available() {
        return Promise.resolve(true);
    }
    navigate(options) {
        return new Promise((resolve, reject) => {
            try {
                const fromToQs = Directions.getFromToQuerystring(options);
                if ((options.ios === undefined || options.ios.preferGoogleMaps !== false) && isAppAvailable("comgooglemaps://")) {
                    utils_1.openUrl("comgooglemaps://" + fromToQs);
                }
                else if (options.ios && options.ios.allowGoogleMapsWeb && options.to instanceof Array && options.to.length > 1) {
                    utils_1.openUrl("http://maps.google.com/maps" + fromToQs);
                }
                else {
                    utils_1.openUrl("http://maps.apple.com/maps" + fromToQs);
                }
                resolve();
            }
            catch (e) {
                reject(e);
            }
        });
    }
}
exports.Directions = Directions;
